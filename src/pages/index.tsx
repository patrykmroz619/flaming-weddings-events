import { AboutSection, MainSection } from '@/components/HomePage';
import Layout from '@/components/Layout';

export const Home = () => (
  <Layout>
    <MainSection />
    <AboutSection />
  </Layout>
);

export default Home;
